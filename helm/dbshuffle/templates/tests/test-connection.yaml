apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dbshuffle.fullname" . }}-test-connection"
  labels:
    {{- include "dbshuffle.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dbshuffle.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
