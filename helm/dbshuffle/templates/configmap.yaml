apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dbshuffle.fullname" . }}
  labels:
    {{- include "dbshuffle.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- .Values.config | nindent 4 }}
